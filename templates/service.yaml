apiVersion: v1
kind: Service
metadata:
  name: {{ include "gogs-full.fullname" . }}
  labels:
    {{- include "gogs-full.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gogs-full.selectorLabels" . | nindent 4 }}
