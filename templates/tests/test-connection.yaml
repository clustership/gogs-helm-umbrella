apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gogs-full.fullname" . }}-test-connection"
  labels:
    {{- include "gogs-full.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gogs-full.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
